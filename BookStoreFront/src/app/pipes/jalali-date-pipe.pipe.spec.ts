import { JalaliDatePipePipe } from './jalali-date.pipe';

describe('JalaliDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new JalaliDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
